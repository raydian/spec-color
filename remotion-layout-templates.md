# Remotion 视频布局模板说明（Markdown 版）

> 适用于 Remotion 框架下的视频页面结构设计。
> 标准画布：1920×1080（16:9）/ 1080×1080（1:1）/ 1080×1920（9:16）
> 帧率：30fps（教学/科普类）/ 24fps（电影类）

---

## 目录

| 编号 | 布局 | 用途 |
|------|------|------|
| S1 | 封面页（Cover） | 开场、主题定调 |
| S2 | 章节分隔页（Section） | 分段、节奏控制 |
| S3 | 单栏正文页（Single Column） | 核心论述、概念讲解 |
| S4 | 双栏/多栏页（Columns） | 并列要点、对比说明 |
| S5 | 图文混排页（Image+Text） | 配图讲解、产品展示 |
| S6 | 数据图表页（Data/Chart） | 指标、趋势可视化 |
| S7 | 公式推导页（Formula） | 数学/物理公式展示 |
| S8 | 流程/时间轴页（Timeline） | 步骤、演进过程 |
| S9 | 对比页（Comparison） | 方案 A vs B |
| S10 | 金句/引用页（Quote） | 强调、情绪收束 |
| S11 | 结束页（Closing） | 致谢、总结 |
| S12 | 中心辐射页（Hub & Spoke） | 概念地图、生态结构 |
| S13 | 矩阵/四象限页（Quadrant） | 优先级、定位分析 |
| S14 | 漏斗页（Funnel） | 转化、筛选流程 |
| S15 | 循环流程页（Cycle） | 闭环、迭代模型 |
| S16 | 公式-案例对照页（Formula+Example） | 定理+例题演示 |
| S17 | 代码展示页（Code） | 编程教程、代码演示 |
| S18 | 图表+文字并排页（Chart+Text） | 数据解读+文字说明 |
| S19 | 列表清单页（List） | 要点罗列、Checklist |
| S20 | 纯文字标题页（Title Card） | 章节标题、过渡 |

---

## S1 · 封面页（Cover）

**适用场景**：视频开场第一帧，建立主题认知。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│                                                  │
│   [学科标签]                                     │
│                                                  │
│        主标题（大字，72-96px）                     │
│        副标题（中号，36-48px，灰）                 │
│                                                  │
│        ───── 红色短分割线 ─────                   │
│                                                  │
│                              [右侧主题 SVG]       │
│                                                  │
│   章节/知识点路径                                │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 背景 | 纯色/渐变底色静帧 | 全程 | — |
| 学科标签 | 从上方滑入 + 淡入 | 0→15 | `translateY(-30px)→0`, `opacity: 0→1` |
| 主标题 | 从下方弹入（spring） | 10→40 | `spring({ damping: 12, stiffness: 100 })` |
| 分割线 | 从左向右绘制（scaleX） | 30→50 | `interpolate(frame, [30,50], [0,1])` |
| 副标题 | 淡入 + 微小上移 | 40→60 | `opacity: 0→1`, `translateY(10px)→0` |
| 右侧 SVG | 从右侧滑入 | 50→80 | `translateX(50px)→0` |
| 路径文字 | 淡入 | 70→90 | `opacity: 0→1` |

**时长**：90 帧（3 秒 @30fps）

**示例代码结构**
```tsx
<Sequence durationInFrames={90}>
  <CoverScene
    title="指数函数"
    subtitle="高中数学必修一"
    subject="数学"
  />
</Sequence>
```

---

## S2 · 章节分隔页（Section）

**适用场景**：进入新章节，控制演讲节奏。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│                                                  │
│        02                                        │  ← 巨大半透明序号
│        核心观点                                  │
│        ─────                                     │
│        三句话讲清我们怎么看市场                    │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 背景 | 满版主色块，从 0 缩放到 1（scale） | 0→20 | `interpolate(frame, [0,20], [0.8,1])` |
| 巨大序号 | 从透明+缩小到正常（scale+opacity） | 15→40 | `opacity: 0→0.3`, `scale: 0.5→1` |
| 章节名 | 从下方滑入 | 35→55 | `translateY(40px)→0` |
| 分割线 | 从左向右绘制 | 50→65 | `scaleX: 0→1` |
| 副标题 | 淡入 | 60→80 | `opacity: 0→1` |

**时长**：120 帧（4 秒）

---

## S3 · 单栏正文页（Single Column）

**适用场景**：一段需要集中表达的论述、定义、结论。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 小标题（章节路径）           页码 / 04            │
│ ──────────                                         │
│ 大标题（本页核心一句话）                           │
│                                                  │
│  正文段落：2–4 行，行高 1.7，宽度 ≤ 70%。         │
│  关键句用主色加粗。                               │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 页眉（小标题+页码） | 淡入 | 0→15 | `opacity: 0→1` |
| 分割线 | 从左向右绘制 | 10→25 | `scaleX: 0→1` |
| 大标题 | 从下方弹入 | 20→45 | `spring({ damping: 14, stiffness: 80 })` |
| 正文行 1 | 淡入 + 上移 | 45→60 | `translateY(15px)→0`, `opacity: 0→1` |
| 正文行 2 | 同上，延迟 10 帧 | 55→70 | — |
| 正文行 3 | 同上，再延迟 10 帧 | 65→80 | — |
| 关键句 | 主色高亮（scale 脉冲） | 80→100 | `spring` 反弹一次 |

**时长**：120 帧（4 秒）

---

## S4 · 双栏/多栏页（Columns）

**适用场景**：并列的 2–3 个要点、优势、模块。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 大标题                                             │
│ ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│ │ 卡片 1   │  │ 卡片 2   │  │ 卡片 3   │        │
│ │ 图标     │  │ 图标     │  │ 图标     │        │
│ │ 标题     │  │ 标题     │  │ 标题     │        │
│ │ 描述 2–3 │  │ 描述 2–3 │  │ 描述 2–3 │        │
│ └──────────┘  └──────────┘  └──────────┘        │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 大标题 | 淡入 | 0→20 | `opacity: 0→1` |
| 卡片 1 | 从下方弹入 | 20→45 | `spring({ damping: 12, stiffness: 100 })` |
| 卡片 2 | 延迟 15 帧弹入 | 35→60 | — |
| 卡片 3 | 再延迟 15 帧弹入 | 50→75 | — |
| 图标（卡片内） | 从缩小到正常 | 25→40 | `scale: 0.5→1` |
| 卡片内文字 | 淡入 | 卡片出现后 10 帧 | `opacity: 0→1` |

**时长**：100 帧（3.3 秒）

---

## S5 · 图文混排页（Image + Text）

**适用场景**：需要配图增强理解（产品截图、架构图、场景照）。

**版式结构（左文右图）**
```
┌──────────────────────────────────────────────────┐
│ 大标题                          ┌──────────────┐ │
│ 要点 1                          │              │ │
│ 要点 2                          │    配图      │ │
│ 要点 3                          │              │ │
│                                 └──────────────┘ │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 大标题 | 淡入 | 0→20 | `opacity: 0→1` |
| 要点 1 | 从左侧滑入 | 15→35 | `translateX(-30px)→0` |
| 要点 2 | 延迟 10 帧 | 25→45 | — |
| 要点 3 | 再延迟 10 帧 | 35→55 | — |
| 图片容器 | 从右侧滑入 + 淡入 | 30→60 | `translateX(50px)→0`, `opacity: 0→1` |
| 图片内容 | 缩放出现（scale 0.9→1） | 40→70 | `interpolate` |

**时长**：100 帧（3.3 秒）

---

## S6 · 数据图表页（Data / Chart）

**适用场景**：业绩、趋势、占比等量化信息。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 大标题（结论前置）                                 │
│  ┌──────────────┐   关键数字：                    │
│  │              │   ▲ +12.4% 营收                 │
│  │   图表区     │   ▼ -3.1%  成本                 │
│  │              │   ● 98%    留存                 │
│  └──────────────┘                                │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 大标题 | 淡入 | 0→15 | `opacity: 0→1` |
| 图表容器 | 从下方滑入 | 15→35 | `translateY(20px)→0` |
| 柱状图/折线 | 从底部生长（scaleY） | 25→70 | `interpolate(frame, [25,70], [0,1])` |
| 数据标签 | 逐条淡入 | 图表绘制完成后 | 每条延迟 10 帧 |
| KPI 数字卡 1 | 从右侧滑入 + 数字滚动 | 40→65 | `translateX(30px)→0` |
| KPI 数字卡 2 | 延迟 15 帧 | 55→80 | — |
| KPI 数字卡 3 | 再延迟 15 帧 | 70→95 | — |

**时长**：120 帧（4 秒）

---

## S7 · 公式推导页（Formula）

**适用场景**：数学/物理公式展示、推导过程。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 定理/公式名称                                      │
│ ─────────────────────────────────────────────    │
│                                                  │
│        f(x) = a·x² + b·x + c                     │
│                                                  │
│   推导步骤：                                       │
│   1. 定义域：x ∈ R                               │
│   2. 对称轴：x = -b/(2a)                         │
│   3. 顶点坐标：(...)                              │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 定理名称 | 淡入 | 0→15 | `opacity: 0→1` |
| 分割线 | 从左向右绘制 | 10→25 | `scaleX: 0→1` |
| 主公式 | 从中央放大出现（scale） | 25→50 | `spring({ damping: 8, stiffness: 120 })` |
| 步骤 1 | 淡入 + 左移消除 | 50→65 | `translateX(-10px)→0` |
| 步骤 2 | 延迟 15 帧 | 65→80 | — |
| 步骤 3 | 再延迟 15 帧 | 80→95 | — |
| 公式高亮 | 当前步骤公式区域发光（opacity 脉冲） | 每步骤显示时 | `spring` 反弹 |

**时长**：120 帧（4 秒）

---

## S8 · 流程/时间轴页（Timeline）

**适用场景**：演进历程、实施步骤、路线图。

**版式结构（横向）**
```
┌──────────────────────────────────────────────────┐
│  ●────●────●────●────●                          │
│  Q1   Q2   Q3   Q4   Q1'                         │
│  调研  立项  试点  推广  复盘                      │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 时间轴线 | 从左向右绘制（scaleX） | 0→60 | `interpolate(frame, [0,60], [0,1])` |
| 节点 1 | 从缩小到正常 + 淡入 | 10→25 | `scale: 0→1`, `opacity: 0→1` |
| 节点 2 | 延迟 20 帧 | 30→45 | — |
| 节点 3 | 再延迟 20 帧 | 50→65 | — |
| 节点 4 | 再延迟 20 帧 | 70→85 | — |
| 节点 5 | 再延迟 20 帧 | 90→105 | — |
| 节点标签 | 节点出现后淡入 | 每个节点后 10 帧 | `opacity: 0→1` |
| 关键节点 | 主色实心圆 + 脉冲发光 | 显示后 5 帧 | `spring` 二次反弹 |

**时长**：150 帧（5 秒）

---

## S9 · 对比页（Comparison）

**适用场景**：方案 A vs B、现状 vs 目标、竞品对照。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│            方案 A              │            方案 B │
│  ──────────────────────────────────────────      │
│  优点：                       │  优点：           │
│   · ...                       │   · ...           │
│  局限：                       │  局限：           │
│   · ...                       │   · ...           │
│  [A 主色标记]                 │  [B 辅色标记]     │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 左侧标题 | 从左侧滑入 | 0→30 | `translateX(-40px)→0` |
| 右侧标题 | 从右侧滑入 | 0→30 | `translateX(40px)→0` |
| 分隔线 | 从上到下绘制 | 20→45 | `scaleY: 0→1` |
| 左侧优点 1 | 淡入 | 35→50 | `opacity: 0→1` |
| 左侧优点 2 | 延迟 10 帧 | 45→60 | — |
| 右侧优点 1 | 淡入 | 35→50 | `opacity: 0→1` |
| 右侧优点 2 | 延迟 10 帧 | 45→60 | — |
| 左侧局限 | 延迟 20 帧淡入 | 65→80 | — |
| 右侧局限 | 延迟 20 帧淡入 | 65→80 | — |
| 标记色块 | 缩放弹入 | 最后 10 帧 | `spring` |

**时长**：120 帧（4 秒）

---

## S10 · 金句/引用页（Quote）

**适用场景**：强调观点、过渡、情绪收束。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│                                                  │
│        "在别人恐惧时贪婪，                         │
│         在别人贪婪时恐惧。"                       │
│                —— 沃伦·巴菲特                    │
│        ───── 主色引号线 ─────                    │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 引号（左） | 从左侧淡入 | 0→20 | `opacity: 0→1` |
| 引文第一行 | 从上方滑入 + 淡入 | 15→40 | `translateY(-20px)→0` |
| 引文第二行 | 延迟 15 帧 | 30→55 | — |
| 署名 | 淡入 + 微小上移 | 55→75 | `translateY(10px)→0` |
| 引号线 | 从左向右绘制 | 70→90 | `scaleX: 0→1` |
| 引号（右） | 淡入 | 85→100 | `opacity: 0→1` |

**时长**：120 帧（4 秒）

---

## S11 · 结束页（Closing）

**适用场景**：致谢、联系方式、后续行动。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│        谢谢观看                                    │
│        Thank You                                  │
│        ─────                                      │
│   联系方式 / 二维码 / 团队署名                     │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 背景 | 渐变淡入（从浅色到深色或反之） | 0→30 | `interpolate` 背景色 |
| "谢谢观看" | 从下方弹入（spring） | 20→50 | `spring({ damping: 10, stiffness: 100 })` |
| "Thank You" | 淡入 | 50→70 | `opacity: 0→1` |
| 分割线 | 从左向右绘制 | 65→85 | `scaleX: 0→1` |
| 联系方式 | 淡入 | 80→100 | `opacity: 0→1` |
| 二维码 | 缩放出现 | 90→110 | `scale: 0.8→1` |

**时长**：120 帧（4 秒）

---

## S12 · 中心辐射页（Hub & Spoke）

**适用场景**：能力地图、生态版图、业务板块、头脑风暴。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│  分支1   分支2   [ 中心主题 ]  分支3   分支4       │
│   │       │         │          │       │         │
│  子项    子项       │         子项    子项        │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 中心节点 | 从缩小到正常（scale） | 0→30 | `spring({ damping: 8, stiffness: 120 })` |
| 连线 1-4 | 从中心向外绘制（scaleX/scaleY） | 20→50 | `interpolate(frame, [20,50], [0,1])` |
| 分支 1 | 从外侧滑入 | 40→60 | `translateX(-30px)→0` |
| 分支 2 | 延迟 10 帧 | 50→70 | — |
| 分支 3 | 再延迟 10 帧 | 60→80 | — |
| 分支 4 | 再延迟 10 帧 | 70→90 | — |
| 子项 | 分支出现后淡入 | 各分支后 15 帧 | `opacity: 0→1` |

**时长**：120 帧（4 秒）

---

## S13 · 矩阵/四象限页（Quadrant）

**适用场景**：优先级排序（重要/紧急）、BCG 矩阵、用户/产品定位。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 纵轴：价值 ↑                                       │
│  ┌─────────────┬─────────────┐                   │
│  │ 象限 II     │ 象限 I（主色高亮）│ 横轴：能力 → │
│  │ 高价值低能力│ 高价值高能力  │                   │
│  ├─────────────┼─────────────┤                   │
│  │ 象限 III    │ 象限 IV      │                   │
│  │ 低价值低能力│ 低价值高能力  │                   │
│  └─────────────┴─────────────┘                   │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 纵轴标签 | 从左侧滑入 | 0→20 | `translateX(-20px)→0` |
| 横轴标签 | 从下方滑入 | 10→30 | `translateY(20px)→0` |
| 外框 | 从上到下绘制 | 20→50 | `scaleY: 0→1` |
| 十字分割线 | 从中央向四边展开 | 40→70 | `scaleX/scaleY: 0→1` |
| 象限 I（高亮） | 背景色填充（主色淡入） | 60→80 | `opacity: 0→0.2` |
| 其他象限 | 浅灰背景淡入 | 70→90 | `opacity: 0→0.05` |
| 象限内标签 | 逐象限淡入 | 各象限绘制后 10 帧 | `opacity: 0→1` |

**时长**：120 帧（4 秒）

---

## S14 · 漏斗页（Funnel）

**适用场景**：转化漏斗、用户筛选、销售管线、候选池收窄。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│   ████████████████████   曝光  100%               │
│    ████████████████       点击   60%               │
│     ████████████          注册   35%               │
│      ████████             付费   12%               │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 第 1 层（最宽） | 从上方下落 + 淡入 | 0→30 | `translateY(-30px)→0`, `opacity: 0→1` |
| 第 2 层 | 延迟 20 帧，同上 | 20→50 | — |
| 第 3 层 | 再延迟 20 帧 | 40→70 | — |
| 第 4 层（最窄） | 再延迟 20 帧 | 60→90 | — |
| 右侧数值标签 | 逐层淡入 | 各层出现后 10 帧 | `opacity: 0→1` |
| 层级间箭头 | 从上方下落 | 随层级 | `translateY` |

**时长**：120 帧（4 秒）

---

## S15 · 循环流程页（Cycle）

**适用场景**：PDCA、增长飞轮、持续迭代、运营闭环。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│        ● 计划 ─────→ ● 执行                       │
│         ↑                  │                      │
│         │                  ↓                      │
│        ● 改进 ←───── ● 检查                        │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 环形路径 | 顺时针绘制（stroke-dashoffset） | 0→60 | SVG `stroke-dashoffset` 动画 |
| 节点 1（计划） | 路径到达后出现（scale） | 10→25 | `spring` |
| 节点 2（执行） | 延迟 30 帧 | 40→55 | — |
| 节点 3（检查） | 再延迟 30 帧 | 70→85 | — |
| 节点 4（改进） | 再延迟 30 帧 | 100→115 | — |
| 箭头 | 沿路径绘制 | 随路径 | — |
| 循环箭头（收尾） | 从节点 4 回到节点 1 的弧线 | 110→130 | `scale` |

**时长**：150 帧（5 秒）

---

## S16 · 公式-案例对照页（Formula + Example）

**适用场景**：定理讲解后立即给出例题。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 定理：二次函数顶点公式                             │
│                                                  │
│  顶点坐标：(-b/2a, (4ac-b²)/4a)                  │
│                                                  │
│ ─────────────────────────────────────────────    │
│                                                  │
│ 例题：求 f(x) = 2x²-4x+1 的顶点                   │
│                                                  │
│ 解：a=2, b=-4, c=1                              │
│     x = -(-4)/(2×2) = 1                         │
│     f(1) = 2-4+1 = -1                           │
│     顶点为 (1, -1)                               │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 定理标题 | 淡入 | 0→15 | `opacity: 0→1` |
| 公式 | 从中央放大出现 | 15→40 | `spring({ damping: 10, stiffness: 100 })` |
| 分割线 | 从左向右绘制 | 40→55 | `scaleX: 0→1` |
| 例题标题 | 淡入 | 50→65 | `opacity: 0→1` |
| 解题步骤 1 | 淡入 + 左移 | 65→80 | `translateX(-10px)→0` |
| 解题步骤 2 | 延迟 15 帧 | 80→95 | — |
| 解题步骤 3 | 再延迟 15 帧 | 95→110 | — |
| 最终答案 | 主色高亮 + 缩放弹入 | 110→130 | `spring` 二次反弹 |

**时长**：150 帧（5 秒）

---

## S17 · 代码展示页（Code）

**适用场景**：编程教程、代码演示。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 代码文件名：index.tsx                            │
│ ─────────────────────────────────────────────    │
│                                                  │
│  import { interpolate, useCurrentFrame }         │
│  from 'remotion';                               │
│                                                  │
│  export const MyComponent = () => {              │
│    const frame = useCurrentFrame();              │
│    const opacity = interpolate(frame,           │
│      [0, 30], [0, 1]                            │
│    );                                            │
│    return <div style={{ opacity }}/>;            │
│  };                                              │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 文件名栏 | 淡入 | 0→15 | `opacity: 0→1` |
| 分割线 | 从左向右绘制 | 10→25 | `scaleX: 0→1` |
| 代码行 1 | 淡入 | 25→40 | `opacity: 0→1` |
| 代码行 2 | 延迟 10 帧 | 35→50 | — |
| 代码行 3 | 延迟 10 帧 | 45→60 | — |
| 当前讲解行 | 高亮背景（scaleX 脉冲） | 每行出现时 | `opacity: 0→0.3` 背景 |
| 光标闪烁 | 在讲解行末尾闪烁 | 讲解行显示期间 | `opacity: 0→1` 每 30 帧切换 |

**时长**：120 帧（4 秒）

---

## S18 · 图表+文字并排页（Chart + Text）

**适用场景**：数据解读+文字说明。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 大标题（结论前置）                                 │
│ ┌──────────────┐  ┌──────────────────────────┐   │
│ │              │  │ 数据解读：                  │   │
│ │   图表区     │  │  · 趋势一：...             │   │
│ │              │  │  · 趋势二：...             │   │
│ └──────────────┘  │  · 趋势三：...             │   │
│                   └──────────────────────────┘   │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 大标题 | 淡入 | 0→15 | `opacity: 0→1` |
| 图表容器 | 从左侧滑入 | 15→40 | `translateX(-30px)→0` |
| 图表内容 | 从底部生长 | 25→70 | `scaleY: 0→1` |
| 文字容器 | 从右侧滑入 | 40→65 | `translateX(30px)→0` |
| 解读点 1 | 淡入 | 60→75 | `opacity: 0→1` |
| 解读点 2 | 延迟 10 帧 | 70→85 | — |
| 解读点 3 | 再延迟 10 帧 | 80→95 | — |

**时长**：120 帧（4 秒）

---

## S19 · 列表清单页（List）

**适用场景**：要点罗列、Checklist、能力清单。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│ 大标题                                             │
│                                                  │
│  [✓] 要点一      一句话说明文字                   │
│  [✓] 要点二      一句话说明文字                   │
│  [✓] 要点三      一句话说明文字                   │
│  [✓] 要点四      一句话说明文字                   │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 大标题 | 淡入 | 0→15 | `opacity: 0→1` |
| 列表项 1 | 从左侧滑入 + 图标打勾动画 | 15→35 | `translateX(-20px)→0`，图标 `scale: 0→1` |
| 列表项 2 | 延迟 15 帧 | 30→50 | — |
| 列表项 3 | 再延迟 15 帧 | 45→65 | — |
| 列表项 4 | 再延迟 15 帧 | 60→80 | — |
| 图标打勾 | 描边绘制（stroke-dashoffset） | 每项出现时 | SVG 动画 |

**时长**：100 帧（3.3 秒）

---

## S20 · 纯文字标题页（Title Card）

**适用场景**：章节标题、过渡、强调关键词。

**版式结构**
```
┌──────────────────────────────────────────────────┐
│                                                  │
│                                                  │
│              核心概念                              │
│                                                  │
│                                                  │
└──────────────────────────────────────────────────┘
```

**动效描述**

| 元素 | 动效 | 帧范围 | 参数 |
|------|------|--------|------|
| 背景 | 渐变切换（从上一场景余韵） | 0→20 | `interpolate` 背景色 |
| 文字 | 从放大到正常（scale） | 15→45 | `spring({ damping: 6, stiffness: 150 })` |
| 文字描边 | 从透明到实色 | 20→50 | `opacity: 0→1` |
| 背景装饰 | 缓慢浮动（轻微上下） | 全程 | `Math.sin(frame * 0.05) * 5` |

**时长**：60 帧（2 秒）

---

## 通用动效规范（所有页面）

### 1. 进入动效类型

| 类型 | 适用场景 | 帧范围 |
|------|----------|--------|
| 淡入（Fade In） | 大多数元素 | 15-20 帧 |
| 上滑入（Slide Up） | 标题、正文 | 20-30 帧 |
| 下滑入（Slide Down） | 副标题、补充说明 | 15-20 帧 |
| 左滑入（Slide Left） | 左侧内容、列表 | 20-30 帧 |
| 右滑入（Slide Right） | 右侧内容、图片 | 20-30 帧 |
| 缩放弹入（Scale Spring） | 重点元素、公式、关键节点 | 25-40 帧 |
| 绘制（Draw） | 线条、分割线、路径 | 15-30 帧 |

### 2. 退出动效类型

| 类型 | 适用场景 | 帧范围 |
|------|----------|--------|
| 淡出（Fade Out） | 大多数元素 | 15 帧 |
| 上滑出（Slide Up） | 正文、列表 | 20 帧 |
| 缩小退出（Scale Down） | 重点元素 | 20-30 帧 |

### 3. 持续动效（循环）

| 类型 | 适用场景 | 实现方式 |
|------|----------|----------|
| 缓慢浮动 | 背景装饰、次要元素 | `Math.sin(frame * 0.05) * amplitude` |
| 脉冲发光 | 高亮节点、关键数字 | `spring` 二次反弹 或 `Math.abs(Math.sin(frame * 0.1))` |
| 光标闪烁 | 代码行、输入框 | `frame % 30 < 15 ? 1 : 0` |

### 4. 延迟规则

- 同级元素依次出现时，每个元素延迟 **10-15 帧**（0.33-0.5 秒）
- 不同层级元素之间延迟 **20-30 帧**（0.67-1 秒）
- 章节切换时，整个场景淡入延迟 **10-20 帧**

### 5. Remotion 动效 API 速查

```tsx
// 线性插值
const value = interpolate(frame, [start, end], [from, to], {
  extrapolateRight: 'clamp',  // 超出范围时钳制
  extrapolateLeft: 'clamp',
});

// 物理弹簧动画
const springValue = spring({
  frame,
  config: {
    damping: 12,    // 阻尼（越大越快停止）
    stiffness: 100, // 刚度（越大越有弹性）
    mass: 1,        // 质量
  },
});

// 缓动函数
import { Easing } from 'remotion';
const eased = interpolate(frame, [0, 30], [0, 1], {
  easing: Easing.easeOutCubic,
});
```

---

## 通用规范（所有页面）

1. **页眉**：小标题 + 页码，建立方位感。
2. **配色**：主色只用于强调（标题线、关键数字、当前项），避免满屏红。
3. **字体**：中文用 `Noto Sans SC / 思源黑体 / 苹方`；数字/英文可用等宽字体突出。
4. **留白**：内容区四周 ≥ 8%，栏间距 ≥ 24px。
5. **一页一义**：每页只传递一个核心信息。
6. **结论前置**：图表页、论述页先把结论写进标题。
7. **字幕安全区**：底部字幕区域保留至少 10% 高度（108px for 1920×1080）。
8. **动效时长**：进入动效 15-40 帧（0.5-1.3 秒），退出动效 15-30 帧。
9. **避免过度动效**：同一页面同时运动的元素不超过 3 个。

---

## Remotion 核心布局组件

### AbsoluteFill
```tsx
import { AbsoluteFill } from 'remotion';

<AbsoluteFill style={{ backgroundColor: 'white', justifyContent: 'center', alignItems: 'center' }}>
  {/* 内容 */}
</AbsoluteFill>
```

### Sequence（时间控制）
```tsx
<Sequence from={0} durationInFrames={90}>
  <CoverScene />
</Sequence>

<Sequence from={90} durationInFrames={150}>
  <ContentScene />
</Sequence>
```

### interpolate（帧动画）
```tsx
const opacity = interpolate(frame, [0, 30], [0, 1], {
  extrapolateRight: 'clamp',
});

const translateY = interpolate(frame, [0, 30], [50, 0]);
```

---

## 快速选用建议

- 开场三件套：**S1 封面 → S2 章节页 → S3 正文**
- 讲观点：**S3 单栏** 或 **S4 多栏**
- 讲证据：**S5 图文 / S6 数据 / S18 图表+文字**
- 讲选择：**S9 对比 / S8 时间轴**
- 讲结构：**S16 公式-案例 / S12 中心辐射**
- 讲转化：**S14 漏斗 / S15 循环流程**
- 讲分析：**S13 矩阵 / S7 公式**
- 讲清单：**S19 列表清单**
- 收尾：**S10 金句 → S11 结束**

---

## 数学教学视频特有布局

| 布局 | 用途 | 参考 |
|------|------|------|
| 公式推导页 | 定理证明、公式推导 | S7 |
| 公式-案例对照页 | 定理+例题 | S16 |
| 图表页 | 函数图像、几何图形 | S6 |
| 代码展示页 | 编程相关数学 | S17 |

---

## 语文/历史教学视频特有布局

| 布局 | 用途 | 参考 |
|------|------|------|
| 图文混排页 | 课文配图、历史照片 | S5 |
| 金句页 | 名句引用 | S10 |
| 列表页 | 知识点罗列 | S19 |
| 时间轴页 | 历史事件演进 | S8 |

---

*文档生成时间：2026-08-05*
*参考：Remotion 官方文档 (https://www.remotion.dev/)*
